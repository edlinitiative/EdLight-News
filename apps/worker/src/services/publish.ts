/**
 * Publish step: process due entries from publish_queue.
 * Not part of MVP — placeholder kept for future IG/WA publishing.
 */
export async function publishDue(): Promise<{ published: number }> {
  return { published: 0 };
}
